-- Host: localhost    Database: character
-- ---------------------------------------------------------------------------
-- Add initial data to character database
--
-- This currently only requires a version to be added.
--

-- Insert character database version -----------------------------------------
REPLACE INTO `character_db_version`
VALUES
    (NULL)
;
